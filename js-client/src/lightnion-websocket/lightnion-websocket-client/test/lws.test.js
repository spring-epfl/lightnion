// Tests for LWS

import { assert } from "chai";
import LWS from "../lightnion-websocket.js";

describe("LightnionWebSocket", function () {
    it("can be imported", function () {
        assert.ok(LWS);
    });
});
